# CHECKPOINT — 下载

最新版本在右侧 **Releases** 里。

- **Mac**：下载 `Checkpoint-macOS-*.zip`，解压后把 `Checkpoint.app` 拖进「应用程序」。
- **Windows**：下载 `Checkpoint-Windows-*.zip`，解压后运行 `Checkpoint.exe`。

游戏启动时会自动检查这里有没有新版本。

本仓库只存放安装包，不含源代码。
